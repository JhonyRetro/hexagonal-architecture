services:
  mongodb:
    image: mongo:latest
    container_name: mongodb-logistics
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=testdb
    volumes:
      - ./mongo-data:/data/db